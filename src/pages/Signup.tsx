import * as React from 'react';

export interface SignupProps {
}

export function Signup(props: SignupProps) {
    return (
        <div>
            Sign up here
        </div>
    );
}
